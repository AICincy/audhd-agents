{
  "allOf": [
    {"$ref": "../_base/schema_base.json"},
    {
      "type": "object",
      "properties": {
        "input_text": {
          "type": "string",
          "description": "Data requirements or pipeline to improve"
        },
        "platform": {
          "type": "string",
          "description": "Data platform",
          "enum": ["bigquery", "snowflake", "databricks", "redshift", "postgres", "custom"]
        }
      },
      "required": ["input_text"]
    }
  ]
}
