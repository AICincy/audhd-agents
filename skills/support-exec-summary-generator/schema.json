{
  "type": "object",
  "properties": {
    "input_text": {
      "type": "string",
      "description": "Source material (reports, research, project data, meeting notes)"
    },
    "audience": {
      "type": "string",
      "description": "Target audience and context level"
    },
    "decision": {
      "type": "string",
      "description": "What the reader needs to decide or understand"
    },
    "framework": {
      "type": "string",
      "description": "Summary framework",
      "enum": ["scqa", "pyramid", "custom"]
    }
  },
  "required": ["input_text"]
}
