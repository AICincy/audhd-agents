{
  "allOf": [
    {"$ref": "../_base/schema_base.json"},
    {
      "type": "object",
      "properties": {
        "input_text": {
          "type": "string",
          "description": "System to optimize or optimization requirements"
        },
        "objective": {
          "type": "string",
          "description": "Optimization objective",
          "enum": ["cost", "latency", "throughput", "accuracy", "availability", "multi-objective"]
        }
      },
      "required": ["input_text"]
    }
  ]
}
