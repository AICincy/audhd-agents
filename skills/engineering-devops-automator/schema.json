{
  "allOf": [
    {"$ref": "../_base/schema_base.json"},
    {
      "type": "object",
      "properties": {
        "input_text": {
          "type": "string",
          "description": "DevOps requirements or pipeline to improve"
        },
        "platform": {
          "type": "string",
          "description": "Target platform",
          "enum": ["gcp", "aws", "azure", "hybrid", "local"]
        }
      },
      "required": ["input_text"]
    }
  ]
}
