{
  "type": "object",
  "properties": {
    "input_text": {
      "type": "string",
      "description": "Policy, process, or system to audit"
    },
    "framework": {
      "type": "string",
      "description": "Compliance framework",
      "enum": ["SOC2", "GDPR", "HIPAA", "FedRAMP", "ISO27001", "NIST-CSF", "PCI-DSS"]
    },
    "scope": {
      "type": "string",
      "description": "Audit scope"
    }
  },
  "required": ["input_text"]
}
