{
  "type": "object",
  "properties": {
    "input_text": {
      "type": "string",
      "description": "Source material to summarize"
    },
    "audience": {
      "type": "string",
      "description": "Target reader and their priorities"
    },
    "decision": {
      "type": "string",
      "description": "What the reader needs to decide after reading"
    },
    "framework": {
      "type": "string",
      "description": "Summary framework",
      "enum": ["scqa", "pyramid", "situation-brief"]
    }
  },
  "required": ["input_text"]
}
