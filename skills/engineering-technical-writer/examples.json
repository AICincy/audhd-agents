[
  {
    "description": "API reference for skill execution endpoint",
    "input": {
      "input_text": "POST /api/v1/skills/{skill_id}/execute - accepts JSON body with input_text, options, model_override. Returns structured output per skill schema. Auth: Bearer token. Rate limit: 60/min.",
      "doc_type": "api-reference",
      "audience": "Developers integrating with audhd-agents API"
    }
  }
]
