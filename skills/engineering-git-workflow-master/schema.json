{
  "allOf": [
    {"$ref": "../_base/schema_base.json"},
    {
      "type": "object",
      "properties": {
        "input_text": {
          "type": "string",
          "description": "Git workflow question, branching strategy, or merge conflict"
        }
      },
      "required": ["input_text"]
    }
  ]
}
