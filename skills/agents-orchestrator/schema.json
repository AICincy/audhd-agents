{
  "type": "object",
  "properties": {
    "input_text": {
      "type": "string",
      "description": "Workflow description or complex task to orchestrate"
    },
    "agents": {
      "type": "string",
      "description": "Available agent pool (comma-separated IDs)"
    },
    "constraints": {
      "type": "string",
      "description": "Budget, time, or model constraints"
    }
  },
  "required": ["input_text"]
}
