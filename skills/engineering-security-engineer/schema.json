{
  "allOf": [
    {"$ref": "../_base/schema_base.json"},
    {
      "type": "object",
      "properties": {
        "input_text": {
          "type": "string",
          "description": "System to secure, threat model, or security review request"
        },
        "scope": {
          "type": "string",
          "description": "Security review scope",
          "enum": ["threat-model", "code-review", "infra-audit", "pentest-plan", "compliance"]
        }
      },
      "required": ["input_text"]
    }
  ]
}
