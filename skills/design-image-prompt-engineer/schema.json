{
  "allOf": [
    {"$ref": "../_base/schema_base.json"},
    {
      "type": "object",
      "properties": {
        "input_text": {
          "type": "string",
          "description": "Image concept or visual requirements"
        },
        "model": {
          "type": "string",
          "description": "Target image generation model",
          "enum": ["dall-e-3", "midjourney", "stable-diffusion", "imagen", "flux"]
        },
        "style": {
          "type": "string",
          "description": "Visual style direction"
        }
      },
      "required": ["input_text"]
    }
  ]
}
