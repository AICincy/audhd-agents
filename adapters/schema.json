{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Adapter Layer",
  "description": "Schema for skill execution through the adapter routing layer",
  "type": "object",
  "properties": {
    "skill_id": {
      "type": "string",
      "description": "Canonical skill ID from skills/ directory"
    },
    "input_text": {
      "type": "string",
      "description": "User prompt or task input to send to the skill"
    },
    "model_override": {
      "type": "string",
      "description": "Optional model alias to override skill default routing"
    },
    "options": {
      "type": "object",
      "properties": {
        "max_tokens": {
          "type": "integer",
          "description": "Maximum tokens in response"
        },
        "temperature": {
          "type": "number",
          "minimum": 0,
          "maximum": 2,
          "description": "Sampling temperature"
        },
        "format": {
          "type": "string",
          "enum": ["json", "text"],
          "description": "Desired output format"
        }
      },
      "description": "Additional execution options passed to the provider adapter"
    }
  },
  "required": ["skill_id", "input_text"]
}
